spring:
  datasource:
    url : jdbc:postgresql://localhost/kasbino
    username : postgres
    password : post457
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
